//2.Assignment operators
// '=' operators;
#include <stdio.h>
int main() {
  int x = 5;
  printf("The '=' operator is store the value in x: %d\n", x);
  
 // '+='addition assignment
    int Num = 5;
	    Num += 3;//Num = Num+3
	  printf("The '+=' operator is store the value in x: %d\n", Num);   
	
 // '-='subtraction assignment
    int Val = 5;
	    Val -= 3;//Val = Val - 3
	  printf("The '-=' operator is store the value in x: %d\n", Val);
	  
 // '*='Multiplication assignment
    int Val_ = 5;
	    Val_ *= 3;//Val = Val * 3
	  printf("The '*=' operator is store the value in x: %d\n", Val_);
	  
// '/='division assignment
    int Val_1 = 5;
	    Val_1 /= 3;//Val_1 = Val_1 / 3
	  printf("The '/=' operator is store the value in x: %d\n", Val_1); 
	  
 // '%='modules assignment
    int Val_2 = 5;
	    Val_2 %= 3;//Val_1 = Val_1 % 3
	  printf("The '%=' operator is store the value in x: %d\n", Val_2);   
	  
	         
	      
  return 0;
}
